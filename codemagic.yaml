workflows:
  ios-workflow:
    name: iOS BloodLink AI
    max_build_duration: 60
    environment:
      flutter: stable
      xcode: latest
      cocoapods: default
    scripts:
      - name: Get packages
        script: flutter pub get
      - name: Build IPA
        script: |
          flutter build ipa \
            --release \
            --no-codesign
    artifacts:
      - build/ios/ipa/*.ipa